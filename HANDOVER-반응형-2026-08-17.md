# 인수인계 — 반응형 정리 작업 (2026-08-17)

> 다음 세션에서 이 파일부터 읽고 이어서 진행할 것.
> 작업 브랜치: `claude/responsive-hardcoding-optimization-hs7uzc`
> **전부 `staging` 에만 반영됨. 정식(main) 무반영.**

---

## 1. 발단 — 사용자 질문

> "페이지 반응형을 하드코딩으로 해놨는데, 전부 픽셀로 바꾼 뒤 일괄 처리하는 게 이득인가?"

조사 결과 **픽셀 전환은 손해**로 결론. 이유:

- 현재 하드코딩된 건 px 가 아니라 **vw(화면 폭 비례)** 다. Webflow 클래스 200개 이상이 글자 크기를 vw 로 잡고 있다.
- vw 는 **바닥·천장이 없어** 화면이 좁아질수록 끝없이 작아진다. 이게 진짜 문제다.
- px 로 바꾸면 유일하게 반응하던 성질이 사라져, 200개 × 화면단 4개 = 800번 손으로 예외값을 넣어야 한다.

**채택한 방향**: 원래 vw 기울기는 두고 `max(하한, 원래값)` 으로 **바닥만 씌운다.** 데스크탑은 건드리지 않는다.
(이 방식은 `faq/faq-responsive.css` 에 선례가 있었다.)

---

## 2. 오늘 staging 에 들어간 것

| PR | 내용 |
|---|---|
| #1420 | `global/viewport.js` — 사이트 자체 뷰포트 기준 신설 (화면 변화 없음) |
| #1423 | `global/viewport-fix.js` — 기기 오분류 교정을 진료과목 전용 → 전 페이지로 |
| #1426 | 서초 — 좁은 화면 글자 하한 |
| #1427 | 서초 — AAHA 뱃지 세로 배치 + 확대 |
| #1428 | 서초 — 뱃지 왼쪽 시각 보정 + 문단 간격 |
| #1429 | 서초 — 문단 간격 확대 |
| #1430 | 서초 — 문단 간격을 전 화면으로 (모바일 한정 해제) |
| #1431 | **서초 — 반응형 규칙을 `seocho.css` 로 합침 (아래 3번 사고 수습)** |
| #1435 | 기기 판정 26곳 → `HelixVP` 한 곳으로 통일 + CLAUDE.md 예방 규칙 |
| #1436 | 홈 — 지점 카드 정보 링크가 폰에서 3px 이던 문제 |

### 되돌리는 지점

```
49fa074   ← 오늘 작업 들어가기 직전
```

---

## 3. ⚠️ 오늘 가장 크게 데인 것 — 반드시 읽을 것

서초 반응형 규칙을 **`seocho/seocho-responsive.css` 라는 새 파일**로 만들었다.
로더(`seocho/bootstrap.js`) FILES 등록도, 워크플로우 퍼지·워밍업 등록도 다 했다.
그런데 **사용자 브라우저에 한 번도 도달하지 않았다.**

그동안 "고쳤습니다" 를 4차례 보고했고, 전부 오보였다. 사용자가 진단 명령을 돌려
`CSS로드 false` 가 나오고서야 발각됐다.

**원인**: 새 파일을 불러오려면 **로더 자신이 갱신**되어야 하는데, 그 로더가 jsDelivr edge 에
옛 버전으로 캐시돼 있으면 새 파일의 존재 자체를 모른 채 예전 FILES 만 불러온다.

**교훈 (CLAUDE.md 에도 기록함)**:

1. 페이지에 CSS/JS 를 더할 때 **새 파일을 만들지 말고** 이미 FILES 에 있는 파일에 이어 붙인다.
2. 새 파일이 꼭 필요하면 **로더 강제 퍼지까지 한 세트**로 처리한다.
3. **"고쳤습니다" 라고 말하기 전에 실제 적용 여부를 확인시킨다.**

### 확인용 진단 명령 (사용자에게 그대로 안내)

```js
(()=>{const c=document.querySelector('.대상클래스');
console.log('화면폭', innerWidth+'×'+innerHeight,
'| CSS로드', [...document.styleSheets].some(s=>/파일이름/.test(s.href||'')),
'| 실제값', getComputedStyle(c).속성);})()
```

캐시 강제 퍼지:

```js
['경로/파일.css'].forEach(f=>fetch('https://purge.jsdelivr.net/gh/pookat73-prog/helixamc-webflow@staging/'+f)
  .then(r=>r.json()).then(j=>console.log(f, j.status||j)))
```

---

## 4. 새로 생긴 것 — 앞으로 쓸 도구

### `global/viewport.js` — 사이트 자체 뷰포트 기준

기준 숫자가 여기 `BANDS` 한 곳에 있다. **바꾸려면 여기만 고친다.**

```
phone-sm ~389 · phone 390~479 · phone-lg 480~767
tablet 768~1039 · laptop 1040~1439 · desktop 1440~1919 · wide 1920~
```

- `<html>` 에 `data-vp` / `data-vp-orient` / `data-vp-short` / `data-vp-tall` 을 붙인다.
- `HelixVP.device()` — 측정용 mobile/desktop. **767 기준 고정. 바꾸지 말 것**
  (GA4 가 이 값으로 나누고 있어 바꾸면 과거 데이터와 비교가 깨진다.)
- tablet 상한을 991 이 아닌 **1039** 로 둔 이유: 아이패드 프로 13"(M4) 세로 폭이 1032.

### `global/viewport-fix.js` — 기기 오분류 교정

Webflow 는 화면 구간(479/767/991)을 바꿀 수 없게 막아 뒀다. 그래서 **기기를 올바른 칸에
밀어 넣는** 방식으로 우회한다.

- 가로로 돌린 큰 폰(900×411) → 화면 폭을 767 이라고 알려 줌 → 가로모바일 판
- 세로 아이패드 프로(1032) → 991 이라고 알려 줌 → 태블릿 판

Webflow 마크업을 건드리지 않으므로 **두 벌이 겹쳐 보이는 깜빡임이 없다.**
되돌리려면 이 파일만 FILES 에서 빼면 된다.

### `global/global.css` 세로 방향 토큰 (아직 아무도 안 씀)

```
--space-y-xs ~ --space-y-xl   세로 여백 (vmin 기준)
--screen-h                     100vh 대신 svh
```

**원칙**: 가로 여백·글자는 vw, **세로 여백은 vmin(짧은 쪽)**.
폭이 좁아지면 내용이 세로로 길어지는데 세로 여백까지 폭에 비례시키면 스크롤이 두 배로 는다.
반대로 가로로 돌린 폰은 폭이 넓다고 글자를 키우면 높이가 모자란다.

---

## 5. 다음에 할 일 (우선순위)

### A. 나머지 페이지 글자 바닥 씌우기 ← 다음 작업

오늘 **서초 · 홈(일부)** 만 했다. 같은 유형의 문제가 다른 페이지에도 있다.

찾는 법: Webflow 에서 해당 페이지 텍스트 클래스의 `font-size` 를 화면단별로 읽어
360px·480px 에서 몇 px 이 되는지 계산한다. **12px 미만이면 손봐야 한다.**

특히 위험한 유형: **`0.85vw` 처럼 vw 인데 좁은 화면 예외값이 하나도 없는 클래스.**
서초 인라인 상담 버튼(4.1px)과 홈 지점카드 링크(3.06px)가 둘 다 이 유형이었다.

남은 페이지: **소개(discover-helix) · 응급(symptoms) · 진료과목(services) · 특화진료**
그리고 홈의 나머지 클래스.

작업 방식: **새 파일 만들지 말고** 각 페이지가 이미 로드하는 CSS 에 이어 붙일 것.

### B. 레이아웃 판정도 HelixVP 로 이관 (선택)

#1435 에서 **측정용 device 판정 26곳**은 통일했다. 아직 남은 것:

```
home/global/buttons.js            var isMobile = innerWidth <= 767
home/global/sections-animations.js  innerWidth <= 767 (레이아웃)
home/section1/section1.js
about/about.js
services/dept-border.js           innerWidth<=767
services/dept-nav.js              innerWidth <= 991
```

이건 애니메이션·레이아웃 분기라 각각 맥락을 읽고 옮겨야 한다. 급하지 않다.

### C. CSS 기준 폭 19가지 정리 (선택)

CSS 미디어쿼리 기준 폭이 아직 제각각이다(420/460/479/520/599/600/720/767/768/850/991/1080).
⚠️ **주의**: 이 임계값을 옮기면 Webflow 의 숨김/보임 전환점(479/767/991)과 어긋나
겹쳐 보이거나 둘 다 사라지는 구간이 생길 수 있다. 옮길 때 그 구간을 반드시 확인할 것.

---

## 6. 사용자 판단 대기 중인 것

| 항목 | 상태 |
|---|---|
| 서초 인라인 상담 버튼 **데스크탑** 크기 | 1440px 에서 12.2px. 작지만 디자인 값이라 안 건드림. 키울지 판단 필요 |
| 오타 "내부 프로토콜**을** 전반에" (서초 AAHA 설명 3번째 문단) | 사용자가 **그대로 두라고 지시** |
| 듀얼 마크업(화면별 중복 섹션 약 30벌) 정리 | **하지 않기로 결론.** 아래 7번 참고 |
| staging → main 승격 | **미승인.** 사용자가 명시적으로 지시할 때만 |

---

## 7. 듀얼 마크업 — 정리하지 않기로 한 이유 (재논의 방지)

화면별로 섹션을 복제해 숨김/보임으로 전환하는 구조가 **약 30벌** 있다
(`About Section 3_M`, `dept-container(M1)`, `Home_Background_3 Mobile` …).

사용자가 "다 켜놓고 코드로 끄고 켜면 되지 않나" 를 제안했으나, **하지 않기로 결론.**

- 코드가 끄면 CSS 가 도착하기 전까지 **두 벌이 겹쳐 보인다**(섹션 통째 깜빡임).
- Webflow 클래스 30개를 파괴적으로 바꿔야 해서 되돌리기 어렵다.
- 복제본 중엔 **내용 자체가 다른 것**도 있다(가로/세로용 배경 이미지 등). 합치면 손해.
- 애초에 아픈 건 듀얼 마크업이 아니라 **글자가 쪼그라드는 것**이었다.

기기 오분류 문제는 `viewport-fix.js` 로 이미 해결했다(마크업 무수정).

---

## 8. 작업 규칙 (이 프로젝트)

- PR base 는 **항상 `staging`**. 만들면 **즉시 squash 머지** (안 하면 사용자가 검증 불가).
- `staging → main` 은 **사용자가 명시적으로 지시할 때만**.
- Webflow 디자인 데이터는 스테이징/정식이 **같은 원본**을 쓴다. Publish 대상만 나뉜다.
  → `data_sites_tool > publish_site` 에 `publishToWebflowSubdomain: true` 로 스테이징만 발행.
- `data_*` MCP 도구는 캔버스 없이 동작. 단 **디자이너를 열어두면 서로 덮어쓴다.**
- Webflow 텍스트는 **맨 안쪽 문자 노드로만** 수정된다. Div Block 이든 Text Block 이든
  요소 자체에는 `set_text` 가 안 먹는다.
- 줄바꿈이 필요하면 `<br>` 대신 **텍스트 블록을 나눠** 각 줄이 독립적으로 접히게 한다.
