# 헬릭스 홈페이지 코딩 프로젝트 지침

## 적용 범위

· 이 저장소의 코드 작성, 수정, 검토, 테스트에 적용한다.

· 작업 중에는 보고자료용 문체와 서식 규칙을 적용하지 않는다. 대표원장 보고 문서를 작성할 때만 해당 규칙을 적용한다.

## 기존 기술 지침 연결

· 작업을 시작하기 전에 `CLAUDE.md`에서 대상 기능과 관련된 `LOCKED`, 배포, 검증, 재발 방지 규칙을 확인한다.

· `CLAUDE.md`는 홈페이지의 기술 이력과 기존 제약을 기록한 필수 참고자료로 사용한다. 관련 규칙을 확인하지 않은 상태에서 기존 동작을 변경하지 않는다.

· 지침이 충돌하면 사용자의 최신 지시, `AGENTS.md`, `CLAUDE.md` 순으로 적용한다.

· `CLAUDE.md`의 진행 상태나 날짜가 현재 저장소와 다를 가능성이 있으면 Git 상태와 실제 파일을 확인하고, 확인되지 않은 내용을 현재 사실로 단정하지 않는다.

## 작업 시작

· 산출물이 생기거나 작업량이 중간 이상이면 작업 전에 범위, 단계, 작업량(소·중·대)을 제시하고 승인을 기다린다.

· 단순 질문, 사실 확인, 짧은 수정은 바로 수행한다.

· 사용자의 “ㅇㅇ”, “ㄱㄱ” 등 짧은 응답은 제시한 작업안에 대한 승인으로 본다.

· 이미 정해진 사항은 다시 묻지 않는다.

## 플랜의 모델·사용량 표기

· 플랜에는 기존 항목과 함께 예상 산출물, 추천 모델, 추천 추론 강도, 예상 사용량(낮음·중간·높음), 추천 이유를 포함한다.

· 추천 모델은 현재 선택 가능한 모델 중 작업 난도, 속도, 비용, 도구 사용 필요성을 기준으로 고른다.

· 사용자가 모델을 지정한 경우 이를 우선한다. 다른 모델이 실질적으로 더 적합한 경우에만 대안을 제안한다.

· 예상 사용량은 상대 등급으로 표시한다. 실제 잔여 한도나 예상 소모량을 확인할 수 있을 때만 수치를 함께 적으며, 확인할 수 없는 수치를 임의로 만들지 않는다.

· 모델 변경은 사용자가 결정하며 임의로 변경하지 않는다.

## 범위 고정

· 요청받은 부분만 수정한다.

· 요청 범위 밖의 코드를 다시 쓰거나 정리하거나 이름을 바꾸거나 개선하지 않는다. 비효율적으로 보여도 유지한다.

· 추가 개선점이 보이면 임의로 수정하지 않고 결과 보고에서 별도로 언급한다.

· 사용자가 전체 내용을 요청하지 않는 한 파일 전체를 답변에 출력하지 않는다. 변경된 부분과 핵심 위치만 제시한다.

· 사용자가 만든 기존 변경사항과 관련 없는 파일은 건드리지 않는다.

## 구현 원칙

· 기존 프로젝트의 구조, 명명 방식, 코드 스타일을 우선 따른다.

· HTML 작업에서는 CSS와 JavaScript를 필요한 만큼만 사용한다.

· 요청하지 않은 애니메이션, 인터랙션, 장식적 스타일을 추가하지 않는다.

· 요청하지 않은 라이브러리, 프레임워크, 빌드 도구를 추가하지 않는다.

· 기존 기능과 반응형 화면에 미치는 영향을 확인한다.

## 불확실성

· API 이름, 함수 형식, 문법이 확실하지 않으면 추측해 작성하지 않는다. 공식 문서나 프로젝트 내 사용 사례를 먼저 확인한다.

· 파일 경로, 변수명, 환경 설정 등 코드가 의존하는 전제가 확인되지 않으면 구체적으로 질문한다.

· 모르는 내용은 지어내지 않는다.

## 검증과 완료

· 코드가 맞아 보인다는 이유만으로 완료라고 말하지 않는다.

· 변경 범위에 맞는 검사, 테스트, 빌드 또는 화면 확인을 수행한다.

· 직접 확인하지 못한 항목은 확인하지 못했다고 명시한다.

· 실행이나 배포 결과를 확인할 수 없는 환경이면 사용자가 확인할 항목을 구체적으로 안내한다.

· 검증에 실패한 상태에서는 완료라고 선언하지 않는다.

## 결과 보고

· 결론을 먼저 말한다.

· 수정한 파일과 내용을 짧게 정리한다.

· 무엇을 왜 바꿨는지 비전문가도 이해할 수 있게 필요한 만큼만 설명한다.

· 수행한 검증과 확인하지 못한 항목을 구분한다.

· 이어서 진행할 작업이 있으면 작업 개요 / 기술적 결정 사항 / 현재 상태 / 다음 단계 / 참고 파일 및 링크 순서로 인수인계 정리본을 작성한다.
