# 인수인계 — 측정 로그 구조 개편 (2026-08-28)

> 받는 사람: 이 작업을 이어받는 다음 세션 (Cowork 등)
> 쓴 사람: Claude Code (원격 세션), 사용자 요청으로 작성
> 함께 읽을 것: `CLAUDE.md` 의 「🗂 원본 로그는 달별 탭으로 나뉜다」 절, `MEASUREMENT-PLAN.md` 의 「측정 요약 시트 구조 개선」 절

---

## 0. 30초 요약

| 항목 | 상태 |
|---|---|
| **하는 일** | 측정 로그가 너무 길어져 요약 시트가 멈추는 문제를 구조적으로 고치는 중 |
| **1단계 — 로그를 달별 탭으로 분리** | 코드 완성·머지 완료. **시트에는 아직 안 붙음** (사용자가 붙여넣어야 함) |
| **2단계 — 시트 구조를 읽는 도구** | 코드 완성·머지 완료. **아직 안 돌림** |
| **3단계 — 요약 표를 스크립트가 직접 그리게 전환** | **미착수. 2단계 결과물이 있어야 시작 가능** |
| **막고 있는 것** | 사용자가 Apps Script 에 파일 3개를 붙여넣고 메뉴 2번 눌러 주기 |
| **⚠️ 오해 금지** | 코드가 머지됐다고 시트에 반영된 게 아니다. Apps Script 는 사람이 붙여넣어야 돈다 |

---

## 1. 배경 — 왜 이걸 하나

### 증상의 역사

측정 요약 시트(`📊 헬릭스 측정 요약`)가 두 번 크게 터졌다. **두 번 다 사이트 코드는 멀쩡했고, 원인은 시트 수식이었다.**

| 언제 | 무슨 일 | 진짜 원인 |
|---|---|---|
| 2026-08-06 | 표가 제목줄만 남고 텅 빔. 사용자는 "클릭수가 전무" 로 읽고 사이트가 고장난 줄 앎 | `QUERY` 안에서 날짜를 날짜 리터럴로 비교 → 숫자(서수)와 타입 불일치 → 조용히 빈 결과 |
| 2026-08-26 | 요약 전체가 `#ERROR! 결과가 너무 큽니다` | `IMPORTRANGE` 가 로그 4만 줄을 통째로 끌어오다 용량 한계 초과 |

두 번째는 로그를 1만 줄씩 8청크로 쪼개 임시 복구했다(`Z1`, `Z10013`, `Z20013` … `Z70013`). 하지만 로그가 **하루 약 1,600줄**씩 늘어 8만 줄에 닿으면 같은 에러가 재발한다. 예상 시점이 **2026년 9월 중순**. 여기에 유입 경로(UTM) 값까지 실리기 시작해 한 줄이 더 길어졌다(`utm_term`/`utm_content` 추가, PR #1469).

### 진짜 병목은 "수식"

`IMPORTRANGE` 용량은 증상이고, 뿌리는 이것이다:

- 측정 파이프라인의 절반이 **스프레드시트 수식**으로 되어 있다 (`IMPORTRANGE` 8벌, 정규식 파싱 7벌, 표 14벌)
- **Claude 는 시트 수식을 쓸 수도, 볼 수도 없다.** Google Drive 커넥터는 `검색 / 읽기 / 다운로드 / 새 파일 생성` 만 지원한다. 공유 권한을 편집자로 열어도 마찬가지 (권한 문제가 아니라 도구 부재)
- 그래서 수식이 고장날 때마다 사용자가 눈으로 읽어 불러주고, Claude 는 짐작으로 처방했다. 8/6 사고 때 사용자가 하루를 날렸다

→ **최상의 해법은 수식을 없애는 것.** 요약 표를 Apps Script 가 직접 그리면 용량 한계도, 조용한 빈 표도, 손편집 왕복도 사라진다. 같은 파일의 `체류·동선` 탭이 이미 그 방식이고 한 번도 안 터졌다.

사용자가 이 방향을 명시적으로 골랐다: **"난 최상의 결과를 만들고 싶어."**

---

## 2. 측정 파이프라인 전체 그림

```
브라우저 (사이트)
  global/ga4-base.js       gtag 정의 · 스테이징이면 no-op
  global/session.js        방문 표식(sid) · 유입처(entry_src) · UTM 5종 부착
  global/sheet-log.js      gtag('event') 를 가로채 시트로도 한 벌 복사
        │  (정식 도메인에서만. 병원 공인 IP 는 애초에 안 보냄)
        ▼  sendBeacon POST
  Apps Script 웹앱  ← 주소는 global/sheet-log.js 의 ENDPOINT 상수
        │            ⚠️ 이 스크립트의 소스는 리포에 없다 (사용자 구글 계정 안에만 있음)
        ▼  appendRow
  원본 로그 스프레드시트 「Helix AMC — 사이트 이벤트 로그」
        [log] 탭 : 시간 | 이벤트명 | page | device | value | params(JSON)
        │
        ├─ IMPORTRANGE ─▶ 「📊 헬릭스 측정 요약」 (수식 기반, 지금 문제의 그곳)
        └─ 스크립트 직접 읽기 ─▶ 같은 파일의 [체류·동선] 탭 (튼튼함, 본보기)
```

### 아는 값

| 이름 | 값 |
|---|---|
| 원본 로그 스프레드시트 ID | `1llPxKf_TyLt2G_DucyQNBzxbzhzGBXyYOhxAMnp_sG0` |
| 요약 스프레드시트 ID | `1VQ84lPsuGGBrz8o6MJH4Bdibw18_fKAxmQWCUtXzYjs` |
| 두 파일 소유자 | `pookat73@gmail.com` (사용자 본인) |
| 웹앱 주소(ENDPOINT) | `global/sheet-log.js` 안에 상수로 있음 |
| GitHub 리포 | `pookat73-prog/helixamc-webflow` |
| 로그 증가 속도 | 하루 약 1,600줄 |

---

## 3. 이번에 한 일 (머지 완료)

### PR #1470 — 로그를 달별 탭으로 나눠 담기

**새 파일 `scripts/sheet-log-monthly.gs`**

- 원본 로그의 `log` 탭에서 **지난달 이전 줄**을 `log 2026-07` / `log 2026-08` … 탭으로 옮긴다
- `log` 탭에는 **이번 달 기록만** 남는다 → 받는 탭이 항상 한 달치 이하
- 메뉴: `지금 상태 보기`(미리보기만, 아무것도 안 건드림) / `지금 달별로 나누기` / `매일 새벽 4시 자동 정리 켜기·끄기`
- 안전장치
  - 옆 탭에 **다 적고 나서야** 원래 자리를 지운다
  - 시간 칸을 못 읽는 줄은 **손대지 않는다** (어느 달인지 모르므로)
  - 흩어진 줄도 아래쪽부터 지워 줄 번호가 밀리지 않는다
  - 한 번에 6만 줄 상한 (실행 시간 6분 제한 회피)
  - 새벽 4시 = 기록이 거의 안 들어오는 시간대

**🔴 기록 받는 웹앱은 일부러 안 건드렸다.** 웹앱을 고쳐 달별 탭에 직접 쓰게 할 수도 있지만, **재배포하다 웹앱 주소가 바뀌면 기록이 통째로 끊긴다.** 뒤에서 덜어내는 방식이면 받는 쪽을 영영 안 건드려도 된다. **이 판단을 뒤집지 말 것.**

**수정 `scripts/sheet-dwell-journey.gs`**

- `readLog_(sinceMs)` 가 머리글에 `이벤트명` 이 있는 탭을 **전부** 읽어 이어붙이도록 변경 (예전엔 한 탭만)
- 조회 기간 밖인 달 탭(`log 2026-07` 등)은 아예 열지 않아 느려지지 않음
- 달별로 나뉜 뒤에도 체류·동선 표가 안 끊긴다

### PR #1472 — 시트 구조 내보내기 도구

**새 파일 `scripts/sheet-structure-export.gs`**

- 메뉴 `[🧰 진단] → [요약 시트 구조 내보내기]` 를 누르면 **모든 탭의 수식과 표 제목**을 글 파일 하나로 뽑아 드라이브에 저장
- 사용자는 파일 이름만 알려주면 되고, Claude 가 `search_files` → `download_file_content` 로 읽는다
- 같은 수식이 아래로 반복되면 `AG1:AG40000 (같은 수식 40000줄)` 로 접는다
- 실려 들어온 로그 영역(Z열~)의 값은 건너뛴다 (읽을 필요 없고 파일만 무거워짐)
- 8만 줄 탭도 5천 줄씩 나눠 읽어 안 멈춘다
- 개인정보가 있는 탭은 `SKIP_VALUE_TABS` 에 넣으면 수식만 뽑는다

**이 도구가 3단계의 전제다.** 요약 표 14개를 코드로 재현하려면 지금 시트에 어떤 수식이 있는지 봐야 하는데, 그걸 볼 방법이 이것뿐이다.

### 검증 방법 (다시 돌려보고 싶을 때)

실제 시트를 건드리지 않고 Apps Script 환경을 흉내 내 확인했다. 스크래치 파일은 세션과 함께 사라지므로, 필요하면 같은 방식으로 재현할 것: `SpreadsheetApp` / `Utilities` / `DriveApp` 을 흉내 낸 객체를 만들고 `eval(fs.readFileSync('scripts/....gs'))` 로 함수를 불러 돌린다.

확인된 것:

- 7월 줄만 `log 2026-07` 로 이동 (구글이 날짜 서식으로 저장한 `Date` 객체 줄 포함), 8월과 시간을 못 읽는 줄은 그대로 남음
- 두 번 돌려도 결과 동일 (같은 작업을 반복해도 안전)
- 흩어진 줄 삭제 정확 (`[h,a,b,c,d,e]` 에서 2·3·5줄 삭제 → `[h,c,e]`)
- `readLog_()` 가 기간 밖 달 탭을 안 열고, 전체 기간이면 다 읽으며, 섞여 든 머리글 줄은 파싱에서 제외됨
- 구조 내보내기: 12,000줄 × 33칸 가짜 시트에서 반복 수식 접기·멀리 떨어진 청크 수식 포착·로그 JSON 제외 모두 확인, 결과 1KB

---

## 4. 지금 상태 — 아직 안 끝났다

**코드는 `main` 에 머지됐지만 시트에서는 아무 일도 일어나지 않았다.** Apps Script 는 사람이 붙여넣어야 도는 물건이라, 리포에 있는 `.gs` 파일은 아직 종이 위의 코드다.

### 사용자가 해야 할 것 (5분)

요약 시트 → `[확장 프로그램]` → `[Apps Script]` 에서 파일 3개를 맞춘다:

| 리포의 파일 | 어떻게 |
|---|---|
| `scripts/sheet-dwell-journey.gs` | 기존 것 **교체** (메뉴 2개가 여기서 만들어짐) |
| `scripts/sheet-log-monthly.gs` | **새 파일**로 붙여넣기 |
| `scripts/sheet-structure-export.gs` | **새 파일**로 붙여넣기 |

저장(Ctrl+S) → 스프레드시트로 돌아가 새로고침(F5) → 메뉴 두 번:

1. `[📁 로그 달별 정리] → [지금 상태 보기]` — 뭐가 몇 줄 옮겨질지만 보여준다. 숫자가 납득되면 `[지금 달별로 나누기]` → `[매일 새벽 자동 정리 켜기]`
2. `[🧰 진단] → [요약 시트 구조 내보내기]` — 만들어진 **파일 이름**을 Claude 에게 알려준다

세 파일이 **같은 Apps Script 프로젝트**에 들어가야 한다 (전역이 공유돼야 메뉴가 다른 파일의 함수를 부를 수 있음). `onOpen` 은 `sheet-dwell-journey.gs` 에만 있다 — 다른 파일에 또 만들면 메뉴가 하나만 살아남는다.

---

## 5. 다음 단계 (3단계) — 요약을 스크립트가 그리게

### 목표 상태

| | 지금 | 바꾼 뒤 |
|---|---|---|
| 로그 옮기기 | `IMPORTRANGE` 8청크 (용량 한계) | 스크립트가 직접 읽음 (한계 없음) |
| 값 뽑기 | 시트 안 정규식 수식 7개 (`AG`~`AQ`) | 코드 |
| 표 만들기 | 수식 14벌 (`COUNTIFS` 9 + `QUERY` 5) | 코드 |
| 지표 변경 | 사용자가 시트에서 손편집 | Claude 가 `.gs` 고치고 끝 |
| 고장 났을 때 | 조용히 빈 표 (발견이 늦음) | 왜 비었는지 탭에 적힘 |

### 안전한 전환 순서 (중요)

1. 사용자가 뽑아 준 **구조 파일**을 읽어, 표 14개가 각각 무엇을 세는지 정리한다
2. `scripts/sheet-summary-build.gs`(가칭)를 만들어 **새 탭**에 표를 그린다 — **기존 요약 탭은 손대지 않는다**
3. 두 탭의 숫자를 나란히 놓고 대조한다. 안 맞으면 새 탭을 지우면 끝이라 되돌릴 것이 없다
4. 다 맞은 뒤에야 기존 수식·`IMPORTRANGE` 를 걷어낸다 (이 단계만 사용자 손이 필요 — 찾기·바꾸기 단위로 줄여서 넘길 것)

### 참고할 본보기

`scripts/sheet-dwell-journey.gs` 가 이미 "스크립트가 로그를 읽어 표를 그리는" 완성된 예다. 로그 읽기(`readLog_`) · 해석(`parseEvents_`) · 방문 묶기(`groupVisits_`) · 표 만들기 · 시트에 쓰기(`writeSheet_`) 가 다 들어 있으니, 새 요약 빌더는 이 뼈대를 그대로 따르면 된다. **결과가 비었을 때 "왜 비었는지"(읽은 탭·줄 수·조회 기간)를 탭에 적어 주는 처리**도 여기 있다 — 반드시 따라할 것. 8/6 사고의 재발 방지책이다.

### 아직 안 하기로 한 것

- **파싱 열 추가** (로그 시트에 `doctor`/`dept`/`sid` 등을 미리 뽑아 두는 안). `MEASUREMENT-PLAN.md` 의 (나) 안이다. 스크립트가 JSON 을 직접 읽으면 필요 없어져서 **보류**. 나중에 사용자가 로그를 눈으로 훑을 때 편하려고 얹는 건 자유
- 요약 시트에 지난달 탭을 가리키는 `IMPORTRANGE` 추가. 3단계를 하면 `IMPORTRANGE` 자체가 사라지므로 **불필요해질 가능성이 높다.** 다만 3단계가 9월 1일보다 늦어지면 그때는 필요하다 (아래 참고)

---

## 6. 시한 — 9월 1일에 주의할 것

달별 정리를 켜 두면 **9월 1일 새벽에 8월치가 `log` 에서 `log 2026-08` 로 빠져나간다.** 그 시점에 3단계가 아직이면, 요약 시트가 `log` 만 보고 있어 **8월 데이터가 표에서 사라진 것처럼 보인다.**

둘 중 하나로 대비:

- (권장) 9월 1일 전에 3단계를 끝낸다 → `IMPORTRANGE` 가 없어져 문제 자체가 없음
- (임시) 요약 시트에 지난달 탭을 가리키는 칸을 하나 추가한다. 탭 이름을 매달 손으로 안 바꾸도록 날짜에서 계산:

```
=IFERROR(IMPORTRANGE($Z$1, "log "&TEXT(EOMONTH(TODAY(),-1),"yyyy-mm")&"!A2:F"), )
```

이건 Claude 가 못 넣는다(도구 부재). 사용자에게 **붙일 칸 하나 + 완성된 수식**으로 줄여서 넘길 것.

---

## 7. 하면 안 되는 것

### 이 작업 고유

- ❌ **기록 받는 웹앱(Apps Script)을 고쳐 재배포** — 주소가 바뀌면 기록이 통째로 끊긴다. 소스도 리포에 없어 되돌리기 어렵다
- ❌ **`log` 탭 이름 바꾸기** — 받는 쪽이 그 이름으로 찾아 쓴다. 바꾸면 기록이 엉뚱한 탭에 쌓이거나 새 `log` 가 생긴다
- ❌ **달별 탭 이름 형식(`log YYYY-MM`) 바꾸기** — 정리 스크립트와 읽는 쪽이 이 형식을 본다
- ❌ **기존 요약 탭을 갈아엎고 시작** — 반드시 새 탭에 나란히 만들어 숫자 대조 후 갈아탄다
- ❌ **`M_KEEP_MONTHS` 를 1 이상으로** — `log` 가 두 달치가 되어 용량 문제가 되돌아온다

### 시트 수식을 만질 때

- `QUERY` 안 날짜 비교는 **반드시 숫자로**. `Col4 >= date '2026-08-01'` ❌ / `Col4 >= "&$AN$1&"` ✅ (`AL`열·`AN`열 모두 서수라 숫자끼리 비교해야 걸린다)
- 수식을 고쳐 결과가 아래로 펼쳐지면 `#REF!` 가 난다 → 아래 블록을 **잘라내기·붙여넣기로 이동**. **행 삽입 금지** (`Z`열부터 로그가 통째로 들어와 있어 같이 밀린다)

### 프로젝트 규칙 (CLAUDE.md)

- 일반 작업 PR 은 **base 를 `staging`** 으로, 사용자 검증 후 `staging → main`
- **측정 관련 변경은 `main` 직행** (스테이징은 도메인 게이트로 측정이 꺼져 있어 검증할 게 없음). 이번 두 PR 도 그래서 `main` 이다 — 사이트에 로드되는 코드가 하나도 없고(전부 `.gs` + 문서) 스테이징에서 볼 것이 없었다
- `claude/* → staging` PR 은 만들었으면 **즉시 머지** (안 하면 사용자가 검증 불가)
- 사용자에게 **Webflow 커스텀 코드를 붙여넣게 하지 말 것.** (Apps Script 붙여넣기는 별개다 — 이건 API 로 쓸 방법이 없어 불가피하고, 사용자도 이미 그 방식으로 쓰고 있다)

---

## 8. 도구 한계 (미리 알고 시작할 것)

| 하고 싶은 것 | 가능? |
|---|---|
| 시트 **값** 읽기 | ✅ Drive 커넥터 `download_file_content` (CSV 는 **첫 탭만** 나온다) |
| 시트 **수식** 읽기 | ❌ → `[🧰 진단]` 도구로 뽑게 한 뒤 그 파일을 읽는다 |
| 시트 칸에 수식 쓰기 | ❌ 도구 자체가 없다. 공유 권한과 무관 |
| Apps Script 소스 읽기·쓰기 | ❌ 사용자가 붙여넣어야 한다 |
| 새 파일 만들기 | ✅ |

원본 로그를 CSV 로 받으면 **첫 탭(`시트1`)이 비어 있어** 아무것도 안 나온다. 값을 직접 세어봐야 할 땐 **요약 시트**를 CSV 로 받으면 `IMPORTRANGE` 된 로그가 `Z`~`AE`열에 딸려와 한 번에 셀 수 있다. (단 이 우회로는 3단계로 `IMPORTRANGE` 를 걷어내면 사라진다 — 그때는 `[🧰 진단]` 도구나 스크립트로 대체할 것)

---

## 9. 파일 지도

| 경로 | 무엇 |
|---|---|
| `scripts/sheet-log-monthly.gs` | 🆕 로그를 달별 탭으로 옮김 |
| `scripts/sheet-structure-export.gs` | 🆕 시트 수식·표 제목을 글 파일로 뽑음 |
| `scripts/sheet-dwell-journey.gs` | 체류·동선 탭 생성 + 상단 메뉴 3개 정의. **스크립트로 표 그리기의 본보기** |
| `global/sheet-log.js` | 브라우저에서 gtag 이벤트를 가로채 시트로 보냄 (웹앱 주소가 여기 있음) |
| `global/session.js` | 방문 표식·유입처·UTM 부착 |
| `global/ga4-base.js` | GA4 기본 + 스테이징 도메인 게이트 |
| `CLAUDE.md` | 프로젝트 규칙 전체. 「🗂 원본 로그는 달별 탭으로」·「📊 요약 시트 QUERY 표가 텅 비면」 절이 이 작업과 직결 |
| `MEASUREMENT-PLAN.md` | 측정 과제 목록. 「측정 요약 시트 구조 개선」 절 |

---

## 10. 사용자에 대해 알아둘 것

- 소통 언어는 **한국어**. 전문 용어를 그대로 던지지 말고 풀어쓸 것 (`CLAUDE.md` 맨 위 「용어 순화」 절이 규칙과 예시를 담고 있다)
- **"고쳤습니다" 를 코드가 맞다는 이유로 말하지 말 것.** 코드가 맞는 것과 화면·시트에 도달한 것은 다르다. 실제 도달을 확인시키기 전에는 "준비됨" 으로 말한다. (과거에 미적용 상태로 4차례 "고쳤다" 고 보고한 사고가 있다)
- 사용자 손이 필요한 일은 **최소로 줄여서** 넘긴다. 7칸 편집을 찾기·바꾸기 2회로 압축해 준 전례가 있다
- 표본이 작을 때 섣부른 결론을 싫어한다. 비율보다 **원 건수**를 먼저 보여주고 표본 크기를 같이 말할 것

---

## 11. 열린 질문

- [ ] 3단계를 9월 1일 전에 끝낼 것인가, 아니면 임시 수식으로 버티고 여유 있게 갈 것인가 — 사용자 결정 필요
- [ ] 요약 표 14개를 **그대로 재현**할지, 이참에 정리해서 줄일지 — 구조 파일을 본 뒤 사용자와 상의
- [ ] `EXCLUDE_SIDS`(운영자 본인 방문 제외 목록)가 비어 있다. 과거 데이터에 병원 내부 방문이 섞여 있을 수 있음 — 3단계에서 같은 제외 장치를 이어갈 것

---

## 부록 — 관련 PR

| PR | 내용 |
|---|---|
| [#1470](https://github.com/pookat73-prog/helixamc-webflow/pull/1470) | 원본 로그를 달별 탭으로 나눠 담기 |
| [#1472](https://github.com/pookat73-prog/helixamc-webflow/pull/1472) | 시트 구조(수식·표 제목) 내보내기 도구 |
| [#1469](https://github.com/pookat73-prog/helixamc-webflow/pull/1469) | 방문 정보에 `utm_term` · `utm_content` 추가 (이 작업의 계기 중 하나) |
